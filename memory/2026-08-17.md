# Daily Log — 2026-08-17

## Mandibular Asymmetry & AI Dev Team Milestones

### 1. AI Team Restructuring & Specialist Integration
- Migrated domain expert bots from Mandibular Asymmetry into centralized team repo `openclaw-ai-dev-team-maxroyak`:
  - `domain_experts/ortho_bot/` (`SOUL.md`, `AGENTS.md`, `SETTINGS.md`)
  - `domain_experts/vision_bot/` (`SOUL.md`, `AGENTS.md`, `SETTINGS.md`)
  - `domain_experts/research_bot/` (`SOUL.md`, `AGENTS.md`, `SETTINGS.md`)
  - `ux_bot/` (`SOUL.md`, `AGENTS.md`, `SETTINGS.md`)
- Merged engineering capabilities into `dev_bot` (React 19/TS + Go backend) and QA gates into `qa_bot` (TestBot authoring + QABot gating).
- Established multi-stage clinical lifecycle and clinical formula change protocol.
- Cleaned up redundant `*Bot.md` files from `mandibular-asymmetry/agents/`.

### 2. Internationalization (i18n) — English & Russian Support
- Added comprehensive typed i18n subsystem in `src/locales/` (`types.ts`, `en.ts`, `ru.ts`, `index.ts`).
- Updated domain layer pure calculation functions (`generateRamusComparison`, `generateBodyComparison`, `generateMandibularAsymmetryConclusion`, `generateClinicalSummary`) to take `locale: "en" | "ru" = "en"`.
- Added `language: Locale` and `setLanguage` action to `studyStore.ts` with `localStorage` (`ma.language`) persistence and automatic report recalculation.
- Created `LanguageSwitcher.tsx` with high-contrast segmented pill toggle (`EN | RU`) in header.
- Updated all UI components: `AnalysisPage`, `ResultsPanel`, `CalibrationPanel`, `ImageViewer`, `LandmarkPalette`, `StudyManager`, `ImageUploadZone`.
- All 295 vitest unit and integration tests passing.

### 3. Track 1: Clinical PDF / Print Export Implementation
- Created `ClinicalReportModal.tsx` rendering a high-contrast 1-page clinical standard report.
- Implemented `@media print` rules in `src/styles/index.css` configured for A4/Letter portrait output with automatic background UI suppression.
- Integrated `Export Report / Print` action buttons into `AnalysisPage` header and `ResultsPanel`.
- Verified crisp vector overlay rendering and quantitative measurement data tables.
- Authored test suite `src/test/reportExport.test.ts`.
- All 300 unit and integration tests passing (100%).

### 4. Track 2: Native DICOM (.dcm) Radiograph Support & Auto-Scale
- Integrated `dicom-parser` with typed definitions in `src/types/dicom-parser.d.ts`.
- Implemented pure client-side DICOM reader (`src/domain/dicom/dicomReader.ts`) with 16-bit/8-bit grayscale VOI windowing, photometric interpretations (MONOCHROME1 / MONOCHROME2), metadata extraction (Patient ID, Study Date), and auto-calibration via Pixel Spacing (`0028,0030`) and Imager Pixel Spacing (`0018,1164`).
- Updated `ImageUploadZone.tsx` to handle `.dcm` drag-and-drop.
- Authored test suite `src/test/dicomParser.test.ts`. 310/310 unit tests passing (100%).

### 5. Track 3: AI-Assisted Landmark Detection via Manual Trigger Button (Phase 2)
- Built pure domain heuristic landmark localization engine in `src/domain/ai/landmarkDetector.ts` and `src/domain/ai/types.ts` generating normalized coordinates and confidence scores for CoR, GoR, CoL, GoL, and Me.
- Added explicit manual trigger `[✨ Auto-Detect Landmarks (AI)]` in `LandmarkPalette.tsx` and `ImageViewer.tsx` toolbar with loading spinner.
- Added clinician-in-the-loop candidate states (`aiCandidateLandmarks`), dashed halo rings on canvas overlay, `[Accept All AI Proposals]` and `[Clear AI Proposals]` actions, and automatic conversion from candidate to verified upon manual adjustment.
- Authored `src/test/aiDetection.test.ts`. 318/318 unit tests passing (100%).

### 6. AI Landmark Detection Accuracy & ROI Cropping Fix
- Added `detectRadiographRoi` filtering out black letterbox/pillarbox padding (luminance < 15) to normalize candidate coordinates relative to active radiograph content.
- Refined anatomical proportional zones: Condylar (Y ∈ [0.15, 0.28], CoR X ∈ [0.12, 0.22], CoL X ∈ [0.78, 0.88]), Gonial (Y ∈ [0.65, 0.78], GoR X ∈ [0.15, 0.25], GoL X ∈ [0.75, 0.85]), Menton (Y ∈ [0.85, 0.93], X ∈ [0.48, 0.52]).
- Added DICOM bypass and updated test suite `src/test/aiDetection.test.ts`. 320/320 unit tests passing (100%).

### 7. UI Refactoring: Relocate "Save Study" and "New Study" Actions to Top Header
- Relocated `[💾 Save Study]` (with unsaved changes pulse indicator) and `[➕ New Study]` (with discard confirmation guard) into central toolbar position in `AnalysisPage.tsx` header alongside the title.
- Streamlined `StudyManager.tsx` sidebar with a clean study status badge and saved studies drawer.
- 320/320 unit tests passing (100%).

### 8. Calibration Points Drag-to-Adjust Functionality
- Enabled interactive dragging and repositioning of Calibration Points P1 and P2 during review stages (`reviewing-point-1`, `reviewing-point-2`, `entering-distance`, and `calibrated`) with coordinate clamping to `[0.0, 1.0]` and real-time scale updating.
- Prioritized calibration marker hit-testing in `ImageViewer.tsx` with grab/grabbing cursor feedback.
- 323/323 unit tests passing (100%).

### 9. Custom User Command
- Configured persistent `token` / `tokens` / `usage` trigger rule in `MEMORY.md` and `AGENTS.md` for instant context headroom & token usage statistics.





